[tool.black]
line-length = 88
target-version = ["py311"]

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
target-version = "py311"
exclude = ["venv", "migrations"]
fix = true

[tool.ruff.lint]
select = ["E", "F", "I"]  # E = pycodestyle, F = pyflakes, I = isort
ignore = []
